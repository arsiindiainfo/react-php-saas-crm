# Google reCAPTCHA v2 site key — public by design (embedded in client JS),
# safe to commit. The matching secret key lives server-side only, in
# backend/.env's RECAPTCHA_SECRET_KEY.
VITE_RECAPTCHA_SITE_KEY=6LfiK6MtAAAAABnEH2scOy7gx-Kf3ZmqNuozbr8Z
